name: style_11_santorini_mediterranean
description: "希臘聖托里尼 / 地中海度假風 (Santorini Mediterranean) Flutter App 範本"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
